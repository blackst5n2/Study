- 진화 배경: 블룸 필터는 데이터가 "있다/없다"를 판단할 때 삭제 기능이 없습니다. (비트 1을 0으로 되돌리면 다른 데이터의 정보가 손상됨)
- 핵심 원리:
	- 일반 블룸 필터의 비트 배열 대신 버킷(Bucket) 배열을 사용함.
	- 충돌이 발생하면, 데이터를 다른 대체 버킷으로 밀어내는(뻐꾸기가 다른 새의 둥지를 사용하는 것처럼) 전략을 사용함.
- 결과: 블룸 필터처럼 $O(1)$의 빠른 탐색 속도와 낮은 오류율을 가지면서도, 데이터의 삭제가 가능함.
- 응용: 캐시 시스템, 분산 K/V 저장소.
