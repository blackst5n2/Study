- 진화 배경: 동적 배열(Vector)은 삽입/삭제가 비효율적이고, 연결 리스트는 캐시 효율이 낮음.
- 핵심 원리:
	- 배열에 데이터 대신 "부분적으로 채원진" 작은 블록들을 배열로 관리함.
	- 삽입/삭제 시 전체 데이터를 밀어내는 대신, **인접한 블록들 사이에서만 재분배(Redistribution)** 을 수행함.
- 결과: 삽입/삭제 시 $O(\log^2n)$ 의 성능을 가지지만, 배열의 연속성을 유지하여 캐시 효율을 극대화함. 이는 동적 배열과 연결 리스트의 장점을 결합한 형태.