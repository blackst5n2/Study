<Window x:Class="Launcher.NicknameDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="닉네임 입력" Height="180" Width="320" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="닉네임을 입력하세요" FontWeight="Bold" FontSize="15" Margin="0,0,0,10"/>
        <TextBox x:Name="txtNickname" Grid.Row="1" Height="30" FontSize="14" Margin="0,0,0,10"/>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="확인" Width="70" Margin="0,0,10,0" Click="BtnOk_Click"/>
            <Button Content="취소" Width="70" Click="BtnCancel_Click"/>
        </StackPanel>
        <TextBlock x:Name="txtStatus" Grid.Row="3" Foreground="Red" Margin="0,10,0,0"/>
    </Grid>
</Window>
